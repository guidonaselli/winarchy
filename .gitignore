# Estado de runtime y backups locales — nunca van al repo
state/
backups/
*.log

# Archivos generados por el theme engine (se regeneran con `winarchy theme set`)
config/komorebi/komorebi.json
config/yasb/styles.css
config/btop/winarchy.theme
config/nvim/winarchy-theme.lua
config/flow/Winarchy.xaml
config/terminal/winarchy-scheme.json
config/ahk/theme.ini
config/pwsh/starship.toml
config/fastfetch/config.jsonc

# Overrides del usuario (locales a cada máquina)
config/ahk/user.ahk
config/pwsh/user.ps1

# Planning interno (specs/changes de OpenSpec y tooling de agentes) — no es parte del producto
openspec/
.claude/
